import LoginForm from "../components/login-form";
import { createClient } from "@supabase/supabase-js";

export default function Page() {
    const registreUser = (user) => {
    }
       
  return (
    <div>
      
    <LoginForm/>
    </div>
  );
}